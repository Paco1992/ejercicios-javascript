const numeroUno = 1;
const numeroDos = "2";

console.log(parseInt(numeroDos)) //esta es una función porque vemos el nombre de parseInt y luego los ()

console.log(numeroUno.toString())//este es un método porque primero pongo el nombre de la variable y que también podría ser un objeto y luego con el punto llamo al método para convertir ese numero en un string

//Básicamente, los métodos son "funciones que pertenecen a una clase". Para llamar a un método utilizas un objeto, en plan "objecto.metodo(arg1, arg2);", mientras que para llamar a una función no necesitas ningún objeto: "funcion(arg1, arg2);