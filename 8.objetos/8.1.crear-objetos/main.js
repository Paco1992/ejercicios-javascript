//Objetos: permiten agrupar las variebles dentro de una misma variable en vez de hacerlo de la siguiente manera:/

const nombre= "Huawei";
const precio= 200;
const stock= true;

console.log(nombre);
console.log(precio);
console.log(stock);

//En cambio en el objeto puedo agrupar las características del producto en una sola variable/

const moviles= {
    nombre: "Samsung", 
    precio: 200, 
    stock: true,};

console.table(moviles);